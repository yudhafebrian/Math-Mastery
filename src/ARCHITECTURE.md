// Clean simplified architecture

// App states: 'home' | 'playing' | 'summary'

// Home: skill cards with lock status (per domain)
// Playing: iterate facts of chosen skill, rotate mode per fact
// Match rounds every 5 facts
// Summary: accuracy/time/mastered status
